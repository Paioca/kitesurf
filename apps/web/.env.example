# ----------------------------------------------------------------------------
# Kitetropos — variáveis de ambiente (apps/web)
# Copie para .env e preencha. NUNCA commite o .env preenchido.
# Cada AMBIENTE (prod / staging / preview) tem seu próprio conjunto de valores.
# Em especial: JWT_SECRET, DATABASE_URL e SUPABASE_* DEVEM diferir entre prod e staging.
# ----------------------------------------------------------------------------

# --- Banco (Supabase Postgres) ---
# Runtime: Transaction pooler (pgbouncer), porta 6543
DATABASE_URL=
# Migrations: Session pooler, porta 5432 (prisma migrate deploy usa esta)
DIRECT_URL=

# --- Storage das fotos: Cloudflare R2 (egress zero) ---
# Credenciais S3 do R2 (Cloudflare > R2 > Manage API Tokens). O endpoint é derivado do
# account id: https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=listings
# Base pública das imagens — domínio custom conectado ao bucket (ex.: https://img.kitetropos.com)
# ou o subdomínio r2.dev (ex.: https://pub-xxxx.r2.dev). SEM barra no fim.
R2_PUBLIC_BASE_URL=

# --- Supabase: banco (Postgres) + storage LEGADO das fotos ---
# SUPABASE_URL/SERVICE_ROLE_KEY ainda usados pela migração de imagens p/ R2 e enquanto
# houver URL antiga no banco. Podem sair depois que a migração for confirmada.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_BUCKET=listings

# --- Auth / API ---
API_PORT=3000
# >= 32 chars. O app TRAVA em prod sem isto. Use um valor DIFERENTE por ambiente.
JWT_SECRET=
JWT_EXPIRES_IN=7d
# OTP_MOCK=true só fora de prod (libera devCode / números de teste)
OTP_MOCK=
OTP_TTL_SECONDS=300
PSP_PROVIDER=mock

# --- SMS (Twilio) — setado na Vercel, não no .env local ---
# Em staging: usar números de teste / allowlist, nunca disparar pra usuário real.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# --- E-mail (Resend) — OTP de login/cadastro por e-mail + links de verificação ---
RESEND_API_KEY=
EMAIL_FROM=

# --- Gate de verificação nas ações de negociação (anunciar / oferta / visita) ---
# Telefone verificado é SEMPRE exigido. E-mail verificado só é exigido com "on".
# Antes de ligar em prod: rodar scripts/diag-verification-gate.mjs e avisar usuários.
VERIFICATION_GATE_EMAIL=off

# --- Observabilidade (Sentry) ---
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
# SENTRY_PROJECT=
# SENTRY_AUTH_TOKEN=
# SENTRY_DEV=true   # liga o Sentry em desenvolvimento
# CSP_REPORT_URI=   # endpoint "Security Header" do Sentry; liga report-uri/report-to da CSP
#   formato: https://oXXXX.ingest.sentry.io/api/<project-id>/security/?sentry_key=<public-key>

# --- App ---
# URL pública do ambiente (prod: https://kitetropos.com ; staging: https://staging.kitetropos.com)
APP_URL=

# --- Analytics (Google Tag Manager) ---
# ID do container GTM (formato GTM-XXXXXXX). O GA4 é configurado DENTRO do GTM, não em código.
# Sem isto, o GTM NÃO carrega. Setar SOMENTE na Vercel de produção — deixar vazio em
# local/staging mantém os dados limpos.
NEXT_PUBLIC_GTM_ID=

# --- Analytics (Meta Pixel) ---
# ID do Pixel (numérico). Inserido direto no código, fora do GTM. Sem isto, o Pixel NÃO
# carrega. Setar SOMENTE na Vercel de produção — mesma lógica do GTM acima.
NEXT_PUBLIC_META_PIXEL_ID=
