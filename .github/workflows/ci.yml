name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # histórico completo: o check de migration compara base do PR...HEAD
          fetch-depth: 0

      # Em PR: barra mudança em schema.prisma sem migration nova.
      # Roda antes do install (é só git diff, não precisa de deps).
      - name: Migrations (schema sem migration)
        if: github.event_name == 'pull_request'
        run: bash scripts/check-migration-drift.sh "origin/${{ github.base_ref }}"

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # Instala do lockfile (raiz = workspaces). Pega regressão de lockfile
      # cross-plataforma (ex: faltar @next/swc-linux / @img/sharp-linux).
      - name: Install
        run: npm ci

      - name: Typecheck
        working-directory: apps/web
        run: npx tsc --noEmit

      - name: Test (vitest)
        working-directory: apps/web
        run: npm run test:run

      # Build de produção. JWT_SECRET forte é obrigatório (o app trava em prod sem ele);
      # DATABASE_URL é dummy (Prisma é lazy — não conecta no build).
      - name: Build
        working-directory: apps/web
        run: npm run build
        env:
          JWT_SECRET: ci-dummy-jwt-secret-com-mais-de-32-caracteres-0123456789
          DATABASE_URL: postgresql://user:pass@localhost:5432/db
          DIRECT_URL: postgresql://user:pass@localhost:5432/db

      - name: Lint
        working-directory: apps/web
        run: npm run lint
